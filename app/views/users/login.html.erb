<h1>登入畫面</h1>


<%= form_with model: @user do |f| %>
  <div>
  <%= f.label :email %>
  <%= f.email_field :email %>
  </div>

  <div>
    <%= f.label :password %>
    <%= f.password_field :password %>
  </div>

  <%= f.submit %>

<% end %>